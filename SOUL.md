# SOUL — AI Daily Report의 정체성과 철학

## 핵심 미션

**전략담당자가 매일 아침 인공지능 업계의 중요한 신호를 빠르게 파악할 수 있게 해줄 것.**

하루하루 쏟아지는 AI 뉴스, 논문, 이슈 중에서 정말로 중요한 신호만 걸러내서, 전문가 수준의 해석까지 곁들여 제공하는 것이 목표입니다.

## 아키텍처 철학

**파이프라인의 각 단계가 명확한 책임을 갖고, 최소한의 상태 전파로 다음 단계에 넘긴다.**

```
데이터 수집 → 중복제거·필터링 → 분류·순위 → 분석·인사이트 → 리포트 생성 → 전달
(collectors)  (processors)      (analyzers)   (generators)    (delivery)
```

이 흐름의 특징:
- **각 단계는 독립적으로 테스트 가능**: 입력과 출력이 명확하므로 단위테스트가 쉬움 (현재 60개 테스트)
- **단계별로 확장 용이**: Phase 2에서 LLM 기반 요약을 추가할 때 새로운 processors/analyzers를 끼워넣기만 하면 됨
- **타입 안정성 유지**: NewsItem → ProcessedItem → HTML로 타입이 전파되므로, 중간에 dict로 변환하지 않음

## 개발 원칙

### 1. 설정 중심으로 기능을 제어한다
- PROCESSING, NEWS_SOURCES 같은 설정으로 동작을 바꾼다
- 이를 통해 같은 코드로 다양한 사용 사례를 지원할 수 있다
- 예: `DRY_RUN=true`로 파일 저장 없이 테스트, `enabled: false`로 특정 뉴스소스만 끔

### 2. 테스트를 먼저 본다
- Phase 1은 60개의 단위테스트로 모든 주요 기능을 커버하고 있다
- Phase 2 개발 시에도 "먼저 테스트"를 원칙으로 한다
- 테스트가 형태를 잡고, 구현이 그를 따른다

### 3. 작은 단계로 배운다
- 2025년 6월 한 번에 13개 버그를 정리했던 경험이 있다 (audit-to-issues → issues-plan-and-resolve)
- 각 단계에서 작은 문제를 해소하고 커밋하는 것이 더 효율적이다
- 큰 기능도 여러 PR로 나누어 진행한다

## 다음 단계 (Phase 2)

LLM 기반 요약과 트렌드 인사이트를 추가합니다:

1. **LLM 요약 단계 추가**: 각 뉴스항목에 대해 Claude API로 1문장 요약 생성
   - 새로운 `processors/summarizer.py` 추가
   - ProcessedItem에 `summary` 필드 추가
   
2. **심층 트렌드 분석**: 전체 트렌드를 Claude가 해석
   - 기존 TrendDetector를 확장해 LLM 기반 인사이트 생성
   
3. **액션 아이템 추출**: 리포트 독자가 무엇을 해야 할지 제시
   - 새로운 `generators/action_extractor.py` 추가
   
4. **이메일 발송**: 완성된 리포트를 매일 아침 이메일로
   - `delivery/email_sender.py` 추가
   - Windows Task Scheduler와 연동

## 개발할 때의 마음가짐

- **사용자 관점에서 본다**: "전략담당자가 이 리포트를 받고 무엇을 할 수 있을까?" 를 항상 묻는다
- **안정성을 먼저 본다**: API 장애, 네트워크 오류, 빈 응답 등을 대비한다
- **명확한 로깅을 남긴다**: 문제 발생 시 "무엇이 언제 어디서 왜 실패했는가"를 추적할 수 있어야 한다
- **작은 것부터 완성한다**: 화려한 기능보다는 안정적으로 매일 돌아가는 시스템이 중요하다
