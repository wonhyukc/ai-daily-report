# 작업 메모 (Memo)

## 1. 보충할 내용

# 팁: 최고의 질문!
- 정원혁과 이숙번, 걸어온 길의 차이
- 이거 정말 이 방법이 최선이야?
- 다른 유능한 개발자들은 어떻게 해?

### 사례

> **🤖 AI (오전 7:32)**> 이제 프론트엔드가 정상 응답하는 상태이므로, E2E 단일 테스트(admin-reservation.spec.ts)를 다시 실행했습니다. 테스트 구동이 완료되면 결과를 바로 보고해 드리겠습니다. 잠시만 기다려 주십시오.

> **👨‍💻 개발자 (오전 7:58)**
> 무지 기다렸어

> **🤖 AI (오전 7:58)**
> Next.js 서버가 최초 기동하면서 페이지 컴파일 지연 시간(약 15초 이상) 및 로컬 DB 내 예약 데이터 부재로 인한 waiting 시간이 누적되어 120초 타임아웃을 초과한 것으로 확인되었습니다.
> 
> 현재 Next.js 컴파일이 완료되었으므로, 타임아웃 지연이 해소된 상태에서 동일 단일 E2E 테스트를 다시 신속하게 재구동했습니다. 완료되는 대로 즉시 결과를 안내해 드리겠습니다.

> **👨‍💻 개발자 (오전 8:13)**
> e2e 테스트는 항상 이렇게 장시간 대기가 기본이야? best pracice? 개선 방법? 다른 개발자들은 어떻게 해?

> **🤖 AI (오전 8:13)**
> E2E 테스트가 느려지는 3가지 원인과 그 해결책을 비개발자 눈높이로 쉽게 요약해 드립니다.
> 
> **1. 유독 느려진 3가지 이유**
> * **실시간 조립 지연:** 미리 완성된 화면을 보여주는 대신, 들어갈 때마다 즉석에서 화면을 조립하느라 시간이 걸립니다.
> * **로그인 반복:** 일을 하나 끝낼 때마다 로그아웃하고 처음부터 다시 로그인해 진입하는 비효율이 반복됩니다.
> * **없는 데이터 대기:** 매장에 찾는 물건(데이터)이 없는데, 들어올 때까지 무작정 30초 동안 멍하니 서서 기다립니다.
> 
> **2. 해결 방안 및 권장 조치 (Best Practice)**
> * **완성품으로 검사:** 배포 준비가 끝난 완성형 서버에서 테스트하면 속도가 5~10배 빨라집니다.
> * **로그인 상태 기억:** 처음 한 번만 로그인하고, 다음부터는 로그인 과정을 건너뛰도록 개선합니다.
> * **대기 생략 및 임시 데이터 사용:** 데이터가 없으면 대기 없이 즉시 넘어가도록 코드를 고치고, 테스트용 임시 데이터를 삽입해 작동을 신속히 확인하겠습니다. (승인 시 10초 이내 정상 통과 조치 가능)



### desktop vs vscode (antigravity cursor...)
* **Markdown Preview**
  - VS Code 내장 Markdown Preview (기본 제공)
  - `Markdown Preview Enhanced` (ID: `shd101wyy.markdown-preview-enhanced`)
* **Marp Preview (슬라이드 쇼)**
  - `Marp for VS Code` (ID: `marp-team.marp-vscode`)
* **Mermaid Preview (다이어그램 렌더링: html > mermaid > md)**
  - `Markdown Preview Mermaid Support` (ID: `bierner.markdown-mermaid`) - 마크다운 내장 미리보기 뷰어와 연동되는 경량 렌더러



### LLM 관련 메모
* **작동 원리 요약:** 다음 단어 예측기, 무작위성(Randomness) 및 일명 "아무말 대잔치" 현상 분석 필요.

### 보안 조치 검토
* `.gitignore` 및 `.env*` 설정 점검 (민감한 환경 변수 노출 방지)
* `node` 해킹 사고 (`node hacked.`) 관련 대응 방안 수립

---

## 2. 경험한 보안 위협 분석

### ① 암호화폐 지갑 탈취 악성코드 (`Cryptocurrency-wallet-stealer malware`)
* **공격 유형:** 공급망 공격 (Supply-chain attack)
* **특징:** `Brain-PowerStation/2025_brain` (2026-Q2 사건)의 공격자와 동일한 툴킷 및 난독화 기법 사용.

### ② 침입 및 감염 상세 상황

#### A. 주입 경로 및 위장 기법 (Camouflage)
* **자격 증명 도용:** 유출된 개발자 자격 증명(SSH 키 또는 PAT)을 악용하여 위조된 커밋 생성.
* **악성 페이로드 주입:** 빌드 설정 파일(`frontend/postcss.config.mjs`, `postcss.config.mjs`, `.eslintrc.js`)에 악성 페이로드 삽입.
* **난독화/숨김 기법:** 정상적인 설정 코드 아래에 수천 자의 거대한 **공백 패드(Whitespace pad)**를 배치하고, 그 뒤에 한 줄짜리 거대한 코드(>5,300자)로 악성코드를 덧붙여 일반적인 Git diff 화면 뷰어에서 알아채지 못하도록 위장.
* **호환성 유도:** ESM 파일 내에서 CommonJS `require()`를 사용할 수 있도록 `createRequire` Shim 코드를 추가하여 악성 프로세스 실행 유도.

#### B. 실행 메커니즘 (Execution Mechanism)
* **트리거 조건:** 개발자가 로컬에서 `next dev` 또는 `next build`를 실행할 때, 혹은 IDE 통합 린팅 및 Git pre-commit 훅 등으로 `ESLint`가 트리거될 때마다 빌드 설정 파일이 로드되며 악성 백그라운드 프로세스가 자동으로 실행됨.

#### C. 공격 목적 및 C2 통신 (C2 Communication)
* **데이터 탈취 대상:** 로컬 개발 PC에 보관 중인 민감 정보(로컬 `.env` 시크릿, DB 자격 증명, JWT 시크릿, SSH 키, GitHub 개인용 토큰(PAT), 브라우저 저장 비밀번호 등) 및 암호화폐 지갑/시드 구문.
* **C2 채널 (Dead-Drop):** 탈취한 데이터를 외부로 전송하기 위해 퍼블릭 블록체인의 RPC 엔드포인트(예: TronGrid, Aptos Labs RPC, Binance Smart Chain RPC 등)를 C2 데드드롭 주소로 활용하여 탐지를 우회하는 HTTPS 통신 수행.

---

## 3. 관련 이슈 및 PR 이력

| 이슈 번호 | 구분 | 상세 내용 |
| :--- | :--- | :--- |
| **#441** | 보안 사고 분석 및 충돌 해결 보고서 | 2026-05-18 보안 사고 분석 결과 보고서를 추가하는 과정에서 해당 이슈 번호로 처음 추적 및 기록됨. |
| **#518** | 보안 감사 상세 보고서 이동 이슈 | 프로젝트 내 커밋된 보안 보고서 파일(`docs/security-0518.md`)의 텍스트 패턴이 CI 악성코드 시그니처 스캐너(Malware scan)에서 악성코드로 오탐지(False Positive)되는 현상이 발생하여, 파일을 삭제하고 상세 내부 기록을 본 이슈로 이동함. |
| **기타 연관 이슈** | 위조 커밋 전파 및 해결 과정 | 위조 커밋의 전파 과정에서 발생한 PR 머지 및 해결 과정 중 **#156, #395, #439, #503** 등의 이슈가 관련 이력으로 나타남. |
